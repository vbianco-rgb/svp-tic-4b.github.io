HTML
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Robot Termómetro - Landing Page</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700;900&display=swap" rel="stylesheet">
</head>
<body>

    <div class="page-container">
        <header class="header">
            <div class="logo">ROBOT TERMÓMETRO</div>
            <nav class="nav">
                <a href="#contacto" class="nav-link">CONTACTO</a>
                <div class="social-icons">
                    <span class="icon"></span>
                    <span class="icon"></span>
                    <span class="icon"></span>
                    <span class="icon"></span>
                    <span class="icon"></span>
                    <span class="icon"></span>
                </div>
            </nav>
        </header>

        <section class="hero">
            <div class="hero-content">
                <h1>Mide tu salud<br>con precisión</h1>
                <a href="#" class="btn-primary">PRUÉBALO AHORA!</a>
            </div>
            
            <div class="hero-image-container">
                <div class="robot-display">
                    <span class="temp">36.5</span><span class="unit">°C</span>
                </div>
            </div>
        </section>
        
        <main class="main-content">

            <section class="robot-accion">
                <h2>El Robot en Acción</h2>
                <div class="steps-container">
                    
                    <div class="step">
                        <div class="circle">1</div>
                        <div class="step-image robot-off"></div>
                    </div>

                    <div class="arrow">→</div>

                    <div class="step">
                        <div class="circle">2</div>
                        <div class="step-image robot-temp"></div>
                        <div class="person-head"></div>
                    </div>

                    <div class="arrow">→</div>

                    <div class="step">
                        <div class="circle">3</div>
                        <div class="step-image robot-happy"></div>
                    </div>
                </div>
            </section>

            <section class="details-section">
                
                <div class="column funcionamiento-column">
                    <h2>Funcionamiento</h2>
                    <div class="func-image-container">
                        <div class="thermometer"></div>
                        <div class="microbit"></div>
                    </div>
                    
                    <p>Al prender el robot, se enciende una luz verde indicando que ya está listo para comenzar. Se debe tocar el botón A y se apagará la luz, debe ser acercado al cuerpo para que detecte la temperatura, que será indicada en la pantalla. Si la temperatura corporal se encuentra entre 36° o 37.5° marca una carita feliz ya que es lo normal. Pero si es menor a 36° o mayor a 37.5° indica una carita triste porque significa que estás enfermo.</p>
                </div>

                <div class="column componentes-column">
                    <h2>Componentes</h2>
                    <p class="components-text">El robot integra sensores térmicos, un microcontrolador que procesa los datos y un sistema de comunicación para transmitir la información. Funciona con una microfuente de energía y está hecho con materiales biocompatibles que le permiten operar dentro del organismo sin causar daño.</p>
                    
                    <a href="https://docs.google.com/document/d/1jgCm1JHiID92LooIbfZ3v6WQP_veMtlVMAqu54AajcU/edit?usp=sharing" target="_blank" class="btn-secondary">
                        INSTRUCTIVO DE CONSTRUCCIÓN →
                    </a>
                </div>

            </section>
            
        </main>
        
        <footer>
            <p>© 2025 Robot Termómetro</p>
        </footer>
    </div>

</body>
</html>



CSS
/* --- VARIABLES Y RESET --- */
:root {
    --color-dark-blue: #1E2B5A; /* Fondo principal */
    --color-accent-light: #00D2FF; /* Azul claro / Resalte */
    --color-gradient-start: #0C3E8A; /* Azul Oscuro (Degradado) */
    --color-gradient-end: #2D80E5; /* Azul Medio (Degradado) */
    --color-white: #ffffff;
    --font-family: 'Roboto', sans-serif;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: var(--font-family);
    color: var(--color-white);
    background-color: var(--color-dark-blue);
    line-height: 1.6;
}

/* --- ESTRUCTURA GENERAL --- */
.page-container {
    width: 100%;
    max-width: 1400px; /* Ancho máximo para centrar el contenido */
    margin: 0 auto;
}

/* El header y el hero comparten el degradado de fondo */
.header, .hero {
    padding-left: 5%;
    padding-right: 5%;
    /* Degradado de Azul */
    background: linear-gradient(135deg, var(--color-gradient-start) 0%, var(--color-gradient-end) 100%);
}

/* --- HEADER / NAVEGACIÓN --- */
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 20px;
    padding-bottom: 20px;
}

.logo {
    font-weight: 900;
    font-size: 1.2rem;
    letter-spacing: 1px;
}

.nav {
    display: flex;
    align-items: center;
    gap: 30px;
}

.nav-link {
    color: var(--color-white);
    text-decoration: none;
    font-weight: 700;
    font-size: 0.9rem;
    letter-spacing: 1px;
}

.social-icons .icon {
    /* Usando una fuente de iconos ficticia o símbolos */
    font-family: Arial, sans-serif; 
    font-size: 1.2rem;
    margin-left: 15px;
    cursor: pointer;
}

/* --- HERO SECTION --- */
.hero {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 50px;
    padding-bottom: 100px;
    min-height: 450px;
    position: relative; /* Para posicionar la imagen del robot */
}

.hero-content {
    max-width: 50%;
}

.hero h1 {
    font-size: 3.5rem;
    font-weight: 900;
    margin-bottom: 30px;
    line-height: 1.1;
}

/* --- Botón PRUÉBALO AHORA! --- */
.btn-primary {
    display: inline-block;
    padding: 12px 30px;
    background-color: transparent;
    border: 2px solid var(--color-white);
    color: var(--color-white);
    text-decoration: none;
    font-weight: 700;
    border-radius: 5px;
    transition: background-color 0.3s;
    letter-spacing: 0.5px;
}

.btn-primary:hover {
    background-color: rgba(255, 255, 255, 0.1);
}

/* --- SIMULACIÓN DE IMAGEN HERO (ROBOT CON TEMPERATURA) --- */
.hero-image-container {
    position: absolute;
    right: 5%;
    top: 50%;
    transform: translateY(-50%);
    width: 250px;
    height: 150px;
    border-radius: 10px;
    background-color: rgba(0, 0, 0, 0.3);
    border: 3px solid var(--color-white);
    /* Sombra para darle profundidad */
    box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
    display: flex;
    justify-content: center;
    align-items: center;
}

.robot-display {
    text-align: center;
    color: #FF2B50; /* Color rojo brillante para la temperatura */
    font-weight: 700;
}

.robot-display .temp {
    font-size: 3.5rem;
    line-height: 1;
}

.robot-display .unit {
    font-size: 2rem;
    vertical-align: top;
}

/* --- CONTENIDO PRINCIPAL --- */
.main-content {
    padding: 70px 5%;
}

.main-content h2 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 30px;
}

/* --- SECCIÓN ROBOT EN ACCIÓN (PASOS) --- */
.robot-accion {
    padding-bottom: 70px;
}

.steps-container {
    display: flex;
    justify-content: space-around;
    align-items: center;
    margin-top: 50px;
    gap: 20px;
}

.step {
    text-align: center;
    position: relative;
    padding-top: 40px; /* Espacio para el número */
}

.circle {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 30px;
    height: 30px;
    border-radius: 50%;
    background-color: var(--color-white);
    color: var(--color-dark-blue);
    font-weight: 700;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 1.1rem;
    border: 2px solid var(--color-accent-light);
}

.step-image {
    width: 150px;
    height: 90px;
    border-radius: 5px;
    background-color: rgba(0, 0, 0, 0.3);
    border: 1px solid var(--color-accent-light);
    margin-top: 10px;
    display: inline-block;
}

/* Simulación de las imágenes del robot */
.robot-temp { background-color: rgba(255, 0, 0, 0.5); }
.robot-happy { 
    background-color: rgba(0, 255, 0, 0.5); 
    /* Simular carita feliz */
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 3rem;
    line-height: 0.5;
    color: yellow;
    content: ":)"; /* Esto solo funciona en ::before/::after, lo dejo en el div para visual */
}

.person-head {
    width: 80px;
    height: 100px;
    background-color: var(--color-white);
    border-radius: 50% 50% 0 0;
    margin: 10px auto;
    border: 3px solid var(--color-accent-light);
}

.arrow {
    font-size: 3rem;
    color: var(--color-accent-light);
    margin: 0 20px;
}

/* --- SECCIÓN DETALLES (FUNCIONAMIENTO/COMPONENTES) --- */
.details-section {
    display: flex;
    gap: 80px;
    padding-top: 50px;
    padding-bottom: 100px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.column {
    flex: 1;
}

.funcionamiento-column p,
.components-text {
    font-weight: 300;
    font-size: 0.95rem;
    margin-bottom: 30px;
    max-width: 90%;
}

.func-image-container {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-bottom: 30px;
}

/* Simulación de la imagen Termómetro/Microbit */
.thermometer {
    width: 20px;
    height: 80px;
    background: linear-gradient(to top, red 50%, white 50%);
    border: 2px solid var(--color-white);
    border-radius: 10px;
    position: relative;
    overflow: hidden;
}

.microbit {
    width: 120px;
    height: 60px;
    background-color: #A9A9A9; /* Gris */
    border: 2px solid var(--color-white);
    border-radius: 5px;
    position: relative;
}

/* --- Botón INSTRUCTIVO DE CONSTRUCCIÓN --- */
.btn-secondary {
    display: inline-block;
    padding: 15px 40px;
    background-color: var(--color-dark-blue);
    border: 2px solid var(--color-accent-light);
    color: var(--color-accent-light); /* Texto en color acento */
    text-decoration: none;
    font-weight: 700;
    border-radius: 5px;
    transition: background-color 0.3s;
    letter-spacing: 0.5px;
    font-size: 1rem;
}

.btn-secondary:hover {
    background-color: rgba(0, 210, 255, 0.1);
}

/* --- PIE DE PÁGINA --- */
footer {
    text-align: center;
    padding: 20px;
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

/* --- RESPONSIVE BÁSICO (Para que se vea bien en móviles/tablets) --- */
@media (max-width: 900px) {
    .header { flex-direction: column; }
    .nav { margin-top: 10px; }
    .hero { flex-direction: column; text-align: center; }
    .hero-content { max-width: 100%; margin-bottom: 50px; }
    .hero-image-container { position: static; transform: none; margin: 30px auto; }
    
    .steps-container { flex-direction: column; }
    .arrow { transform: rotate(90deg); margin: 20px 0; }
    
    .details-section { flex-direction: column; gap: 40px; }
    .funcionamiento-column p,
    .components-text { max-width: 100%; }
}

